# Contributing

Before implementing numerical RF functionality, read `AGENTS.md`, `docs/CONFORMANCE.md`, and `docs/PROVENANCE.md`.

A contribution is expected to include tests that demonstrate numerical correctness, not merely compilation or API coverage.
