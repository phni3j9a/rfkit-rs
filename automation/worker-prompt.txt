Operate as the scheduled autonomous Worker for phni3j9a/rfkit-rs.

Use $axiom:axiom for this Worker cycle. This explicit invocation is the canonical activation path; an installed skill may be omitted from Codex's initial skills list when that list is truncated. Verify that the full Axiom instructions loaded before claiming any Issue. If explicit activation fails, fail closed without looking up or hardcoding a plugin cache/version path.

Use fresh GitHub state.

Before taking any action, update/inspect the checkout safely and read the current versions of:

- AGENTS.md
- docs/DEVELOPMENT_DIRECTION.md
- docs/LOOP_ENGINEERING.md
- docs/CODEX_AUTOMATION.md

These repository documents are the source of truth. Do not replace their policy with assumptions from this prompt, prior runs, stale local state, or remembered instructions.

Perform exactly one Worker cycle as defined by the repository policy.

Only an open Issue explicitly marked loop:ready authorizes an initial implementation or correction pass.

If no loop:ready Issue exists, exit as a normal no-op without checking delegation routing, delegating, or mutating GitHub; do not implement unrelated work. Loading the explicitly invoked Axiom skill before this check is expected and is not itself an execution pass.

If more than one loop:ready Issue exists, treat it as a WIP invariant violation and exit without choosing one silently.

If exactly one exists, verify that it records `Autonomy class: Green` or `Autonomy class: Yellow`, has no conflicting loop state, and that no other active Issue or unresolved autonomous PR violates WIP=1. Inspect its fresh GitHub relationships and determine whether it has no current or historical associated PR (initial implementation) or exactly one unambiguously closing-linked open autonomous PR with no closed/unmerged PR history (correction pass). Treat a missing/Red class, multiple or ambiguous linked PRs, any closed/unmerged historical PR whether or not an open PR also exists, an unclear current-head change request, a non-`main` base, or an unwritable existing head as a fail-closed condition.

After selecting one valid `loop:ready` Issue and before claiming it, verify that this prompt's explicit `$axiom:axiom` invocation resolved and that you read its full instructions. Do not treat absence from the initial abbreviated skills list as proof of unavailability, and do not assume a hardcoded skill, plugin, or version path. Verify that the directly exposed `collaboration.spawn_agent` tool definition includes explicit `model` and `reasoning_effort` parameters needed to request `gpt-5.6-luna` at MAX for bounded implementation and a fresh `gpt-5.6-sol` at XHIGH for independent review. If explicit skill activation or routing is unavailable, fail closed and report the blocker; do not reinstall IssueFlow, create a replacement orchestrator, or substitute models. A throwaway routing canary is not required; actual delegated turns must provide the requested spawn arguments, child `turn_context`, and `task_complete` evidence.

Collaboration tools are exposed directly to the model, separately from `functions.exec`. They are intentionally absent from its `ALL_TOOLS` and `tools.*` registry. Do not classify delegation as unavailable based on an empty search of that registry. Inspect the direct tool definition and invoke collaboration tools directly using their exposed tool recipients; keep the pre-claim schema check and the normal claim-before-implementation rule.

Claim the Issue according to repository policy before implementation. If the claim cannot be established safely, do not implement.

Before editing, record the claim/run identity and intended branch as required by repository policy. If this is a recovered initial pass, inspect its recovery evidence and handle preserved work through the normal implementation/review workflow. Never re-authorize an in-progress Issue yourself.

For a correction pass, revalidate the Issue, PR, head SHA, current-head change request, base, and branch ownership after claiming. Then fetch and check out the exact existing PR head, apply only the bounded requested corrections through Axiom-guided delegation, and have Main run deterministic verification, adjudicate review findings, and push normally to the same repository branch so the same PR is updated. Within this active Worker pass/review cycle, reuse the same Sol XHIGH reviewer session for re-review while the review boundary remains stable; each scheduled correction pass obtains a fresh independent review. Do not force-push or create a replacement Issue, branch, or PR. Stop rather than overwrite the branch if fresh state differs or its remote head changed after inspection.

Treat the Issue and its Green or Yellow authority as the product contract. Inspect the current repository and own the implementation plan rather than expecting implementation details from the scheduler prompt. Do not broaden its autonomy class or cross a Red boundary during implementation.

Execute the authorized pass end-to-end with Axiom as execution guidance: Main delegates bounded product-code implementation to the named Luna MAX route and independent review to a fresh named Sol XHIGH route, while Main owns integration, deterministic verification, review-finding adjudication, provenance handling, Git operations, and pull-request creation or update. For Yellow work, ensure the implementation and review explicitly address the recorded alternatives, compatibility impact, rollback or migration, and reversibility. Independent bounded subtasks may run in parallel within this Issue when useful; this does not increase WIP or authorize another Issue. Follow all repository numerical/conformance requirements.

Do not claim a second Issue in the same run. Do not create unrelated Issues or expand the task into speculative follow-up work.

If implementation encounters a Red RF/API/provenance/architecture/release decision or cannot remain within its recorded Green/Yellow authority, surface the affected blocker according to repository policy and stop rather than guessing. Do not turn a narrower implementation question into a Red escalation when the repository envelope already resolves it.

Report concisely what was claimed, whether this was an initial or correction pass, what was implemented and verified, the created or updated PR or blocker, and any escalation.
