# Direct dependencies for the reproducible scikit-rf oracle harness.
# Keep these pins exact: generators and exact contract fields are checked
# against canonical bytes; S-to-Z output values use the fixture's recorded
# numeric tolerance.
numpy==2.5.1
scikit-rf==2.0.1
