# scikit-rf oracle

This directory will contain reproducible Python tooling that generates and/or executes differential test cases against a pinned scikit-rf version.

The first Codex milestone should build this harness before broad feature porting begins.
